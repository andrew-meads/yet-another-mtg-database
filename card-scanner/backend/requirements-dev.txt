# Development / test dependencies (host venv, the Docker `test` stage, CI).
-r requirements.txt
pytest>=8
pytest-cov>=5
# Starlette's TestClient needs httpx.
httpx>=0.27
# Linter + formatter for this Python folder (the repo's `npm run lint` excludes it).
ruff>=0.6
