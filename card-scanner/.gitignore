# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# --- Node ---
node_modules/
frontend/dist/

# --- Runtime output: saved card crops ---
data/cards/*
!data/cards/.gitkeep

# --- Env / editor ---
.env
.DS_Store

# --- Scryfall image cache (standalone mode), harness overlays, tooling caches ---
data/scryfall-cache/
test-images/_overlays/
backend/.ruff_cache/
backend/htmlcov/
backend/.coverage
# OCR models downloaded for host runs (`make models`); the Docker image fetches its own.
backend/models/
# Generated synthetic datasets (standalone-mode paths; the root .gitignore covers data/synth*/ at the repo root only)
data/synth*/
